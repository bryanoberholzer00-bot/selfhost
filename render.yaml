services:
  - type: web
    name: mammamia-mfp
    runtime: docker
    dockerfilePath: ./mammamia/Dockerfile
    rootDirectory: ./mammamia
    envVars:
      - key: TMDB_KEY
        value: da_inserire
      - key: PROXY
        value: da_inserire
      - key: FORWARDPROXY
        value: da_inserire
